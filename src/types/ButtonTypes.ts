export type NormalButtonProp = {
  clickHandler?: () => void,
  noAnimation?: boolean,
  children?: React.ReactNode,
  style?: String,
};

export type ConnectWalletButtonProp = {
  clickHandler?: () => void,
  noAnimation?: boolean,
  children?: React.ReactNode,
  style?: String,
};
